import React from 'react';
import styled from 'styled-components';
const Signatures = () => {
    return (
        <StyledSignatures>
            
        </StyledSignatures>
    );
}

export default Signatures;